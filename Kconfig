mainmenu "nuvix Configuration"

menu "General setup"

comment "nuvix uses a single .config in the repository root."

endmenu

source "kernel/Kconfig"

# ARCH comes from the build system, which selects the architecture before any
# other target is built.
source "arch/$(ARCH)/Kconfig"

source "fs/Kconfig"
